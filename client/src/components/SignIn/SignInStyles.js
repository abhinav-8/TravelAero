import { makeStyles } from "@material-ui/core";

export default makeStyles((theme) => ({
  mainContainer: {
    display: "flex",
    flex: "1 1 auto",
    justifyContent: "center",
    alignItems: "center",
    height: "100%",
  },
    cardContainer: {
      height: "20%",
  },
  cardItemsContainer: {},
}));
